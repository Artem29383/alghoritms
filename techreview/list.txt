1) Написать функцию flatten (task1)
2) Расскажи что в твоем понимании замыкание в js?
3) Задача на замыкание (task2)
4) Расскажи про this? Чем отличаются стрелочные функции от обычных? С помощью каких методов можно управлять контекстом? (Расскажи о них)
5) (task3, первый уникальный символ)
6) Очередь порядка вызова, что вызывется раньше (task 4) // Расскажи про промисы?
7) Что такое virtual DOM в реакте,  зачем он нужен, как реакт понимает,  что пора осуществить ререндер?
8) Работал ли с хуками в реакте? (если да, вопросы про хуки, перечисли с какими работал?)
8.1. Расскажи про useEffect, в чем отличие useEffect от useLayoutEffect?
8.2 Представь ситуацию, есть родительский и дочерний компонент, в родительском компоненте есть коллбек, который через пропсы передается дочернему
и вызывается внутри, нужно сделать так,чтобы рендер дочернего компонента вызывался только при явно меняющихся пропсах (task5).
9. Зачем реакту ключи?
10. задача реакт
Необходимо исправить хук, в который через пропсы пробрасывается счётчик, данный хук каждую секунду выводит
строку (дата,  счётчик), при увеличении счётчика интервал вывода строки меняться не должен (task6)
(import { useEffect, useRef, useState } from "react";

                  export const useTimeout = (count) => {
                    const interval = useRef(null);
                    const [time, setTime] = useState(count);

                    useEffect(() => {
                      clearInterval(interval.current);
                      interval.current = setInterval(() => {
                        setTime(count);
                        console.log(count);
                      }, 1000);
                    }, [count]);
                  };
)
11. Разница между cookie, localstorage, sessionStorage?
